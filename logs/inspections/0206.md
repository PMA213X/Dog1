# 四足 RL 训练巡检报告（第三轮）

- **时间**：2026-09-26 02:06
- **判定情况**：D（venvs/rl 的 torch 安装卡死）→ 改用备选 venv 执行**情况 B 动作**，冒烟训练**已成功完成**

---

## 1. 进程

| 进程 | 状态 |
|---|---|
| `train_ppo` | 02:04–02:05 短暂运行（冒烟，PID 965561），已正常退出 |
| `webots` / `rl_agent` | 仅在冒烟期间由 walk_env 自动拉起，随训练结束退出 |
| `pip install torch` (PID 940212) | ⚠️ **仍存活但卡死**：状态 `Dsl`，WCHAN=`vfs_unlink`，已运行 27+ 分钟，无任何进展 |

## 2. 依赖

### 任务指定 venv：`Ubuntu2/venvs/rl`（Python 3.14.4）— 不可用
- `import torch` → `ModuleNotFoundError`
- `import stable_baselines3` → `ModuleNotFoundError`
- 根因：**ntfs3 内核 bug**。01:34 内核日志出现
  `kernel BUG at fs/iomap/buffered-io.c:1061!`（`Comm: pip`, PID 933424，写入 NTFS 分区时触发）。
  当前 pip 940212 卡在 `vfs_unlink`（不可中断 D 状态），大概率是同一 ntfs3 问题的后续——**kill 不掉，等内核恢复或重启才能清理**。
- 旧日志 `logs/pip_torch.log`（01:36，PID 936328）尾部还有 `OSError: 超出磁盘配额`（tmpfs /tmp 当时被占满）。
- `webots-sim/rl/walk_env.py` ✅ 存在（18192 字节，01:32）。

### 备选 venv：`/tmp/rl_venv`（Python 3.11.16）— ✅ 就绪
- torch 2.14.0+cpu ✅、stable-baselines3 2.9.0 ✅、gymnasium 1.3.0、numpy 2.4.6
- `PPO` 可导入，`torch.cuda.is_available()` = False（CPU 训练，符合预期）
- 创建于 00:53，位于 tmpfs，**不受 ntfs3 bug 影响**。

## 3. 日志 / 训练进度（冒烟）

- 日志：`logs/train_smoke.log`（02:04 启动，**0 个 Traceback/ERROR**）
- 自检：reset/step 通过，耗时 1.37s；obs=42 维，action=12 维
- 结果：
  - **2048 步全部完成，耗时 1.2 分钟，约 29.6 steps/s**
  - `ep_len_mean = 1000`，`ep_rew_mean = 658`
  - TensorBoard：`runs/ppo_walk_20260926-020446/PPO_1`
  - 模型已保存：`checkpoints/ppo_walk_final.zip`（1.9 MB，02:05）✅（此前 checkpoints/runs 均为空）
- 环境链路验证：walk_env 自动拉起 Webots + rl_agent（TCP 桥接）→ 训练 → 正常关闭，全链路通。

## 4. 资源

| 项 | 值 |
|---|---|
| GPU | RTX 4060 Laptop，26/8188 MiB，0%，50°C（冒烟用 `--device cpu`，未用 GPU） |
| 内存 | 14 GiB 总 / 10 GiB 已用 / 4.3 GiB 可用；Swap 9/19 GiB |
| 磁盘 | `/` 47%；数据盘 74%（剩 447G）；`/tmp` 73%（5.4G/7.5G，其中 ball_detect 3.4G、旧 rl_venv 解包 1G、cuDNN whl 474M） |
| 内核告警 | 01:34 ntfs3 iomap kernel BUG；00:33 webots-bin 曾 GPF 崩溃一次 |

## 5. 行动决策与执行

- **venvs/rl 卡死（情况 D，但原因非"慢"而是 ntfs3 内核 bug）**——pip 卡在 D 状态无法 kill，继续等无意义。
- 发现 `/tmp/rl_venv` torch+sb3 齐全 → **执行情况 B：启动冒烟训练，已成功**。
- 未执行情况 C（sb3 已在备选 venv 就绪）。

### 遗留问题（下轮处理）
1. **PID 940212 pip 卡死**：D 状态不可 kill，需重启后清理；或等 ntfs3 恢复。
2. **venvs/rl（NTFS 上）不可靠**：ntfs3 有 kernel bug，后续正式训练建议直接用 `/tmp/rl_venv`，或把 venv 迁到 ext4（如 `~/.venvs/rl`）。注意 `/tmp` 是 tmpfs（7.5G），装 CUDA 版 torch 会撑爆；CPU 版目前占用可接受。
3. **正式训练未启动**：冒烟已通过，下一步可跑长训练（如 `--total-steps 200000`），checkpoint 已有 `ppo_walk_final.zip` 可续可重。
4. `/tmp/ball_detect` 3.4G + 旧 pip 解包 1.7G 可清理回收 tmpfs 空间。
