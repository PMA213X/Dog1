# RL 训练巡检 — 01:04

## 进程状态

- **无训练进程**：`pgrep -af 'train_ppo|webots|rl_agent'` 未发现 `train_ppo.py`、Webots 或 `rl_agent` 进程。
- **并行补齐进程（正常工作中）**：
  - PID 894384/894385：`pip install --user --break-system-packages torch --index-url https://download.pytorch.org/whl/cu130`，正在下载 CUDA 依赖（已下到 `nvidia-nccl-cu13` 216MB / `nvidia-cudnn-cu13` 553MB 等大件）；torch 完成后会接着装 `gymnasium stable-baselines3 tensorboard opencv-python-headless`。
  - PID 893240：依赖 watcher，每 120s 轮询 `walk_env.py` 是否存在 + torch/sb3 是否可导入，最多 15 次（约 30 分钟），检测到就绪后退出。

## 日志摘要

- `logs/` 仅有空的 `inspections/`、`reports/` 子目录；`runs/` 为空。
- `find logs/ runs/` 无任何 `*.log` / `*.txt` / `events*` 文件——**尚无训练日志产出**。
- 安装日志在 `/tmp/pip_torch.log`、`/tmp/pip_sb3.log`（系统临时目录，不在仓库内）。

## 训练进度

- `runs/` 空，无 TensorBoard `events*` 文件。
- `checkpoints/` 目录存在但为空，无 `.zip` / `checkpoint*` / `.pth`。
- **训练尚未开始，无任何 checkpoint 可续。**

## RL 代码状态

| 项 | 状态 |
|---|---|
| `webots-sim/rl/config.py` | ✅ 存在（80 行，OBS_DIM=42, ACTION_DIM=12, PPO 超参齐全） |
| `webots-sim/rl/train_ppo.py` | ✅ 存在（18264 字节，支持 `--dry-run` / `--resume`） |
| `webots-sim/rl/play.py` | ✅ 存在（13490 字节） |
| `webots-sim/rl/README.md` | ✅ 存在 |
| `webots-sim/rl/walk_env.py` | ❌ **缺失**（`QuadrupedWalkEnv` 环境文件不存在） |
| `.venv/bin/python` | ✅ Python 3.12.14，但 **无 pip**，且 torch / sb3 均未安装 |
| 系统 Python 3.14.4 | torch / sb3 均未安装（安装进行中，`--user` 方式） |
| mock 环境 | `webots-sim/rl/.mock_test/` 内有冒烟测试用 mock `walk_env.py` 及假 checkpoint，**非真实 Webots 环境** |

## 系统资源

| 资源 | 状态 |
|---|---|
| GPU | RTX 4060 Laptop，8GB 显存，占用仅 26MiB / 0%，空闲（CUDA 13.0, driver 580.178.04） |
| 内存 | 14Gi 总量，11Gi 已用，可用 3.6Gi；Swap 19Gi 用了 8.4Gi（偏紧但可用） |
| 磁盘 | `/` 84G/189G（47%）；数据盘 1.2T/1.7T（74%，剩 450G）——磁盘充足 |

## 阶段判断

**未开始 / 等待依赖（情况 C）**

- 不属于 P1–P4 任一训练阶段：训练进程未启动，`runs/`、`checkpoints/` 均为空。
- 近集奖励均值、存活步数：**无数据**（尚无任何训练/评估日志）。
- 阻塞项：
  1. `webots-sim/rl/walk_env.py` 缺失（真实 Gym 环境，需对接 Webots）；
  2. torch + stable-baselines3 未安装完毕（并行进程正在装，CUDA 版 torch 下载量大，预计还需数分钟到十几分钟）。
- 就绪判定 watcher（PID 893240）正在轮询，两项就绪后可启动训练。

## 异常/问题

1. `walk_env.py` 缺失是当前主要阻塞——根据会话记忆，该文件由并行子代理（general-28）负责补齐，本次巡检**未重复派发**，避免冲突。
2. torch 装到**系统 Python 3.14**（`--user --break-system-packages`），而 `.venv` 是 Python 3.12 且无 pip——两条环境路线并存，启动训练时必须用**系统 `python3`**（而非 `.venv/bin/python`），否则 import 不到 torch/sb3。README 中的 venv 说明与实际安装方式不一致。
3. `pip_sb3.log` 出现过一次 PEP 668（externally-managed-environment）报错痕迹，是**未带 `--break-system-packages` 的早前尝试**；当前命令（PID 894384）已带该参数，torch 部分正常下载中，尚无最终结果。
4. 内存 11Gi/14Gi + swap 8.4Gi 已偏高，训练启动后 Webots + PPO 并跑需留意 OOM。
5. 无历史失败记录（无日志可查），不属于情况 D（反复失败）。

## 下一步行动

1. **等待**并行进程完成：torch/sb3 安装结束（看 `/tmp/pip_torch.log` 尾部出现 `Successfully installed`）+ `walk_env.py` 落盘；watcher PID 893240 会自动检测。
2. 就绪后验证：
   ```bash
   python3 -c 'import torch, stable_baselines3; print(torch.__version__, stable_baselines3.__version__, torch.cuda.is_available())'
   python3 webots-sim/rl/train_ppo.py --dry-run   # 需显示 walk_env 状态"就绪"
   ```
   注意用系统 `python3`，不是 `.venv/bin/python`。
3. 先跑小规模冒烟：`python3 train_ppo.py --total-steps 2048 --device cpu --eval-interval 0`，通过后再放大步数。
4. 若 30 分钟后 `walk_env.py` 仍未出现或 pip 失败 → 升级为明确阻塞，派子代理补 `walk_env.py` / 改用 CPU 版 torch（约 200MB，避开 CUDA 大包下载）。
5. 下轮巡检重点：训练进程是否已启动、`runs/` 是否出现 `events*`、近集奖励与存活步数。
