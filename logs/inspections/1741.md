# 巡检报告 17:41 UTC（本地 01:41，第二轮）

## 一、进程状态

| 项 | 状态 |
|---|---|
| train_ppo / 训练进程 | ❌ 未启动 |
| watcherv（PID 893240） | ❌ 已消失（上轮就绪 watcher 已退出） |
| pip 旧进程（894384 等） | ❌ 全部死亡，共 3 次安装尝试失败 |
| **pip CPU torch（PID 940212）** | ✅ 运行中，已下载完毕进入 `Installing collected packages` 阶段 |
| Webots | ⚠️ 有独立任务在跑 `parkour_dev.wbt`（timeout 120，PID 927587，与 RL 训练无关，未干预） |

## 二、依赖状态

| 依赖 | 状态 |
|---|---|
| `webots-sim/rl/walk_env.py` | ✅ **已落盘**（18KB，01:32 更新）——情况 C 解除，无需派子代理 |
| venv `Ubuntu2/venvs/rl`（Python 3.14.4） | ✅ 已建好，gymnasium 1.3.0 / numpy 2.5.3 就绪 |
| torch | ⚠️ 安装中（CPU 版，PID 940212） |
| stable-baselines3 | ❌ 未装（待 torch 完成后装） |

## 三、失败复盘（3 次安装全挂）

1. `pip_torch.log`（CUDA 版）：nvidia-cublas 下载 6 次断流 → `incomplete-download` 失败。
2. `pip_sb3.log`：cudnn 下载到 526.9/553.1MB → `OSError [Errno 122] 超出磁盘配额`。
3. 并行代理的 cu130 版（PID 932310，`--resume-retries 20`）：同样 `Errno 122 配额` 失败。

**配额错误为假警报**：`df /` 97G 空闲、inode 8%、文件系统无 quota 特性、fstab 无 quota 项。真实原因应是 pip 临时目录（tmpfs `/tmp` 4.5G/7.5G 已用，CUDA 大件解压超出）或缓存写入受限。对策：本次 CPU 安装显式指定 `TMPDIR=.piptmp` + `PIP_CACHE_DIR` 于数据盘。

## 四、上轮遗留问题的修正

- 0104 报告称"torch 装到系统 Python 3.14 + `--user`"——**实际路线已变**：并行代理新建了 `Ubuntu2/venvs/rl` venv，所有安装应使用 `$VENV/bin/pip`、训练用 `$VENV/bin/python`，**不是**系统 python3，也不是 `.venv`。
- `walk_env.py` 由并行代理（general-28）补齐完成，本轮**未重复派发**。

## 五、系统资源

- GPU：RTX 4060 Laptop，26/8188 MiB，0%，51°C，空闲
- 内存：9.3/14 GiB，swap 10/19 GiB（偏高，训练并跑 Webots 需留意）
- 磁盘：`/` 47%（97G 余）；数据盘 74%（450G 余）

## 六、日志/进度

- `runs/`、`checkpoints/` 均为空 → 从未有训练产出
- 近 90 分钟新文件：`logs/STATUS.md`（monitor 01:25 自动刷新）、`0104.md`
- `logs/STATUS.md` 提示历史日志有 1 处 OOM 关键词命中（旧日志，未阻塞）

## 七、行动决策：情况 D（安装失败→降级 CPU 版）+ 等待

已执行：
- 放弃 CUDA 版（3 次失败），启动 CPU 版 torch 安装（PID 940212，脱离 shell，`/tmp/pip_torch_cpu2.log`），TMPDIR/缓存均指向数据盘。
- 因另一并行代理也在操作同一 venv，本轮**未**同时启动第二个 pip（避免 site-packages 写冲突）。

下一步（torch 装完后，按序）：
```bash
VENV=/run/media/pma213x/0C6A8CC86A8CAFCE/Ubuntu2/venvs/rl
$VENV/bin/pip install stable-baselines3
$VENV/bin/python -c 'import torch, stable_baselines3; print(torch.__version__, stable_baselines3.__version__)'
# 冒烟训练（CPU，无 GPU torch 时不能用 --device cuda）：
cd /run/media/pma213x/0C6A8CC86A8CAFCE/Ubuntu2/CodeU/Dog1
nohup $VENV/bin/python webots-sim/rl/train_ppo.py --total-steps 2048 --device cpu --eval-interval 0 > logs/train_smoke.log 2>&1 &
```

风险提示：
- CPU torch 意味着冒烟与后续训练暂用 `--device cpu`；RTX 4060 空闲，若要 GPU 需另择时机装 CUDA 版（建议 `--resume-retries 20` + 数据盘 TMPDIR 重试）。
- 若本次 CPU 安装再次报 Errno 122，则确认为环境级配额问题，需排查 pip 配置/内核 quota。
- 有并行代理活跃（编辑 config.py/train_ppo.py、跑 Webots 测试），启动训练前需再次 `pgrep pip` 防冲突。
