# RL 训练巡检 — 17:42（含故障处置）

## ⚠️ 发现并修复：P4 死锁

### 症状
- 日志 7+ 分钟无增长、CPU 时间冻结（00:05:22 不变）
- 进程卡在 `poll_schedule_timeout`（等 TCP 桥响应）
- 但 checkpoint 仍在涨（最新 160k @ 17:17）→ 卡在两次 checkpoint 之间

### 诊断
- P4 进程 1975712 死锁于 Webots TCP 桥（端口 11453）
- Webots 1975906 仍在跑但不响应
- 训练实际进度 **172,032 / 200,000**（86%），奖励 1,950

### 处置
1. kill 卡死的 P4 + 旧 Webots 进程
2. 从 `checkpoints/p4_stairs_160000_steps.zip` 续训
3. 新进程 PID **2229737**（CPU 28%~94%，活跃）
4. 日志恢复增长：`本次运行 40000 步（目标总步数 200000）`
5. 评估端口冲突（Address already in use）→ 非致命，跳过评估

### 当前状态
| 项 | 值 |
|---|---|
| P4 PID | 2229737（新） |
| 进度 | 160k → 200k（剩 40k 步） |
| 续训 checkpoint | `p4_stairs_160000_steps.zip` |
| 错误 | 0（仅评估端口冲突，已跳过） |

## 结论
🟢 已恢复 — P4 死锁已修复，续训中。
